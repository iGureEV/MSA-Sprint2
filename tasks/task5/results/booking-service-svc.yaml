# task5/booking-service-svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: task4-booking-service
spec:
  selector:
    app: booking-service  # ← выбирает ВСЕ поды (v1 и v2)
  ports:
    - port: 80
      targetPort: 8080
