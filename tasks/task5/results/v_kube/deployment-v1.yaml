apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service-v1
  namespace: task4-booking-service
  labels:
    app: booking-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: booking-service
      version: v1
  template:
    metadata:
      labels:
        app: booking-service
        version: v1
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
      - name: booking-service
        image: task4-booking-service:1.0
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        env:
        - name: ENABLE_FEATURE_X
          value: "false"
        - name: VERSION
          value: "v1"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /ping
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ping
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
